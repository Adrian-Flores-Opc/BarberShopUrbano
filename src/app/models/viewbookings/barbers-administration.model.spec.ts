import { BarbersAdministration } from './barbers-administration.model';

describe('BarbersAdministration', () => {
  it('should create an instance', () => {
    expect(new BarbersAdministration()).toBeTruthy();
  });
});
