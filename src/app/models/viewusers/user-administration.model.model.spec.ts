import { UserAdministrationModel } from './user-administration.model.model';

describe('UserAdministrationModel', () => {
  it('should create an instance', () => {
    expect(new UserAdministrationModel()).toBeTruthy();
  });
});
