import { UserAuthentication } from './user-authentication.model';

describe('UserAuthentication', () => {
  it('should create an instance', () => {
    expect(new UserAuthentication()).toBeTruthy();
  });
});
